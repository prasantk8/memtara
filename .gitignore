# Noir
/circuits/target/
**/target/

# Rust (memtara-vault is a library crate; lockfile isn't committed by convention)
/vault/target/
/vault/Cargo.lock

# macOS
.DS_Store

# Backend: per-circuit bb verification keys — a build artifact of
# circuits/, regenerated at boot by verify::ensure_vkeys, not source.
/backend/api/vkeys/
